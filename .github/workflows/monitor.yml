name: Tesla Model Monitor

on:
  schedule:
    # 6시간마다 실행 (UTC 기준 0, 6, 12, 18시)
    - cron: "0 0,6,12,18 * * *"
  workflow_dispatch: # 수동 실행 지원

permissions:
  contents: write

jobs:
  monitor:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        working-directory: tesla-model-monitor
        run: |
          pip install -r requirements.txt
          playwright install chromium
          playwright install-deps chromium

      - name: Run monitor
        working-directory: tesla-model-monitor
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: xvfb-run python monitor.py

      - name: Commit & push state changes
        working-directory: tesla-model-monitor
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add state.json
          git diff --cached --quiet || git commit -m "chore: update state.json [skip ci]"
          git push
